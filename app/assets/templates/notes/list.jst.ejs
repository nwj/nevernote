<h1><%= Nevernote.current_name %></h1>
<ul>
  <% Nevernote.notes.each(function(note) { %>
  <li>
    <a href="#/notes/<%= note.get('id') %>">
      <%= note.escape('title') %>
    </a>
  </li>
  <% }); %>
</ul>
